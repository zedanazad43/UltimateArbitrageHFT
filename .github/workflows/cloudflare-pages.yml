# Deploy the React frontend to Cloudflare Pages (free tier)
name: Deploy frontend to Cloudflare Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cloudflare-pages
  cancel-in-progress: true

jobs:
  deploy-frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: projects/dashboard/package-lock.json

      - name: Install frontend deps
        working-directory: projects/dashboard
        run: npm ci || npm install

      - name: Build frontend
        working-directory: projects/dashboard
        env:
          REACT_APP_BACKEND_URL: https://api.ecostamp.net
        run: CI=false npm run build

      - name: Add SPA fallback for Cloudflare Pages
        run: printf '/*    /index.html   200\n' > projects/dashboard/build/_redirects

      - name: Create Cloudflare Pages project (if missing)
        uses: cloudflare/wrangler-action@v3
        continue-on-error: true
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: 652e53f35781522e2745784cc4425d9d
          command: pages project create ultimate-arbitrage-frontend --production-branch=main || true

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: 652e53f35781522e2745784cc4425d9d
          command: pages deploy projects/dashboard/build --project-name=ultimate-arbitrage-frontend --commit-dirty=true
