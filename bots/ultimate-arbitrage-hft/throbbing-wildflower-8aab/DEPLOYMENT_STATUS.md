# Deployment Status

Status: reference-only snapshot for feature comparison.

This directory is not the active production deployment source.
Active source-of-truth for emergent release is `/app`.

Do not run production deploys from this directory.
