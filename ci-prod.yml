name: CI/CD Secure Production Bot

on:
  push:
    branches: [main, "release/**", refactor/bots-unified-setup, develop]
  pull_request:
    branches: [main, "release/**"]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write

jobs:
  build-test-audit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Audit security & autofix
        run: |
          npm audit
          npm audit fix || true

      - name: Lint code quality
        run: |
          if npm run | grep -q lint; then npm run lint; else echo "No linter."; fi

      - name: Run unit/integration tests
        run: |
          if npm run | grep -q test; then npm test; else echo "No tests defined."; fi

      - name: Upload test coverage (اختياري)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage # عدّل المسار إذا كنت تستعمل أداة تغطية مختلفة

      - name: Build Project
        run: npm run build

      - name: Deploy to Cloudflare
        if: github.ref == 'refs/heads/main'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: npm run deploy

      - name: Notify On Failure (Slack Example) # تفعيل عند إعداد سر token
        if: failure()
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}

  dependabot:
    runs-on: ubuntu-latest
    if: github.actor == 'dependabot[bot]'
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm test || true
      - run: npm audit || true