# ============================================================
# Vercel env vars — Frontend (Stampbook / dashboard)
# Copy into Vercel dashboard → Settings → Environment Variables
# Build-time var (must be set before build).
# ============================================================

# Points the frontend at the deployed backend (Railway 8787 URL)
REACT_APP_BACKEND_URL=

# Example: https://ultimate-arbitrage-hft.up.railway.app
