============================================================
CLOUDFLARE DNS ROUTE — uahft-gateway tunnel
============================================================

⚠️ SUPERSEDED (2026-09): the Go engine now runs as a Cloudflare Container
at https://hft-engine.<account>.workers.dev (see projects/go-engine/wrangler.toml
+ .github/workflows/deploy-engine.yml). wrangler.toml points HFT_ENGINE_URL at
that container, so the gw.ecostamp.net tunnel route below is NOT required.
The record can still be added if you want a branded engine URL, but the worker
will keep using the workers.dev host until HFT_ENGINE_URL is changed.
Add this ONE CNAME manually in the Cloudflare dashboard
(ecostamp.net → DNS → Records → Add record):

  Type:    CNAME
  Name:    gw
  Target:  0acfa7ee-d02a-4b86-84e8-762b1c4bd445.cfargotunnel.com
  Proxy:   ON (orange cloud)   [enables Cloudflare WAF/caching]

Result: https://gw.ecostamp.net  ->  uahft-gateway  ->  http://127.0.0.1:8080

Tunnel ID: 0acfa7ee-d02a-4b86-84e8-762b1c4bd445
Tunnel created: 2026-07-11
Status: CONNECTED (fra17)

WHY MANUAL: the shell's CLOUDFLARE_API_TOKEN returned HTTP 401
(invalid/expired), so `cloudflared tunnel route dns` hangs. A fresh
token (written to C:\Users\azadz\.cf_dns_token, NEVER pasted in chat)
would let the agent finish this via CLI instead.

Alternative CLI step once a valid token exists:
  cloudflared tunnel route dns uahft-gateway gw.ecostamp.net
============================================================
