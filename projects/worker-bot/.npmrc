# @browserbasehq/stagehand@1 (pulled in via @langchain/community) pins peer
# openai@^4 while this project uses openai@^6. The conflict is upstream and
# cannot be resolved by pinning, so we install with legacy peer resolution.
legacy-peer-deps=true
