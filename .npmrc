# Monorepo-wide npm config. projects/worker-bot depends on @langchain/community
# (which pulls @browserbasehq/stagehand pinning peer openai@^4) while using
# openai@^6 — an unresolvable upstream peer conflict, so use legacy resolution.
legacy-peer-deps=true
